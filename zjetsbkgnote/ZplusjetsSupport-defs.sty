\newcommand{\comment}[1]{}

\def\thadhad{ \ensuremath{\tau_{\mathrm{had}}\tau_{\mathrm{had}}} }
\def\tlhad{ \ensuremath{\tau_{\ell}\tau_{\mathrm{had}}} }
\def\tll{ \ensuremath{\tau_{\ell}\tau_{\ell}} }

\def\Ztt{ \ensuremath{Z\to\tau\tau} }
\def\Zll{ \ensuremath{Z\to\ell\ell} }
\def\Htt{ \ensuremath{H\to\tau\tau} }
\def\VBFHtt{ \ensuremath{(VBF)H\to\tau\tau} }
\def\ggHtt{ \ensuremath{(GGF)H\to\tau\tau} }
\def\VHtt{ \ensuremath{(V)H\to\tau\tau} }
\def\HWW{ \ensuremath{H\to WW} }

\def\thad{\ensuremath{\tau_{\mathrm{ had}}}}

\def\MMC{\ensuremath{m_{\tau\tau}^{\mathrm{MMC}}}}

\def\bdtscore{BDT$_{\text{score}}$}

\def\dtilde{ \ensuremath{\tilde{d}} }
\def\dtildeB{ \ensuremath{\tilde{d}_{B}} }

\def\oo{ \ensuremath{O_{1} } }
\def\ootwo{ \ensuremath{O_{2} } }
\def\signdphi{ \ensuremath{\Delta\phi^{\mathrm{sign}}_{jj} } }

\def\pt{p_{T}}
\def\mt{m_{T}}

\def\atk{ anti-$k_t$ }

\def\sintheta{\ensuremath{\sin \theta_{W}  }}
\def\sintheta{\ensuremath{\sin 2\theta_{W}  }}
\def\costheta{\ensuremath{\cos \theta_{W}  }}
